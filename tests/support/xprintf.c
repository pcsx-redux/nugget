// Built here rather than from common/libc so that the object, which carries
// the tests' defines, does not land in the directory psyqo and openbios
// also compile it into.
#include "common/libc/xprintf.c"
